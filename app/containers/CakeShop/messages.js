/*
 * CakeShop Messages
 *
 * This contains all the text for the CakeShop container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CakeShop';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CakeShop container!',
  },
});
